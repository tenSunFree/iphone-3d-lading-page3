import { backgroundPng } from "../utils/index";

const Footer = () => {
  return (
    <div className="w-full">
      <img className="bg-[#FFFFFFFF]" src={backgroundPng} alt="backgroundPng" />
    </div>
  );
};

export default Footer;
